# Copy to .env for local dev. NEVER commit the real .env.
# In production these are set in GitHub Actions Secrets and Cloudflare env vars.

ANTHROPIC_API_KEY=sk-ant-...
NEWS_API_KEY=                 # optional; we have RSS fallbacks
RESEND_API_KEY=               # phase 6 only
SITE_URL=https://realdailyreview.com
DRY_RUN=0                     # set to 1 to skip writes
